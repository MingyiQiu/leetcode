# 0145. Binary Tree Postorder Traversal

🔗 [LeetCode](https://leetcode.com/problems/binary-tree-postorder-traversal/)

## Approach

Use a **Recursive Depth-First Search (DFS)** approach to traverse the binary tree in postorder.

- Create an empty list `result` to store the traversal order.
- Define a recursive helper function `dfs(node)`.
- If the current node is `None`, return.
- Recursively traverse the left subtree.
- Recursively traverse the right subtree.
- Visit the current node by adding its value to `result`.
- Start the traversal from the root node.
- Return `result` after the traversal is complete.

Postorder traversal follows the order: **Left → Right → Root**.

## Complexity

- Time Complexity: **O(n)**
- Space Complexity: **O(n)**

> The recursion stack can grow up to `O(n)` in the worst case (a completely skewed tree).

## Code

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

class Solution:
    def postorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        result = []

        def dfs(node):
            if not node:
                return

            dfs(node.left)
            dfs(node.right)
            result.append(node.val)

        dfs(root)
        return result
```
